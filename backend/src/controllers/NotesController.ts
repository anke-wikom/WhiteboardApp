/*
app.get('/notes/', (req, res) => {
    return res.send(Object.values(req.context.models.notes));
  });
*/

// export const handleGetNotes = (req, res) => {
//     return res.send(Object.values(req.context.model.notes));
// };